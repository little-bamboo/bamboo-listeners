import json
import requests
import scrapy
import


if __name__ == '__main__':

# Pull arguments from command line
# Collect URLs from page
# iterate through each url and transform URL into raw format ingest
# Store each document into mongodb
